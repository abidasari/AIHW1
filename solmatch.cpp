#include <string>
#include <map>
#include <iostream>
#include <fstream>
#include <iomanip>
#include <sstream>

int main()
{
	ifstream infile, expfile;
	infile.open("output.txt");
	expfile.open("expectedop.txt");
	string s1, s2;
	while(!inp.end())
	{
		getline(inp, s1);
	}

}